{
  "name": "videotogif",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p vendor && cp -r node_modules/@ffmpeg/ffmpeg/dist/esm vendor/ffmpeg && cp -r node_modules/@ffmpeg/util/dist/esm vendor/ffmpeg-util && cp -r node_modules/@ffmpeg/core/dist/esm vendor/ffmpeg-core"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.6",
    "@ffmpeg/ffmpeg": "^0.12.6",
    "@ffmpeg/util": "^0.12.1",
    "express": "^4.18.2"
  }
}
